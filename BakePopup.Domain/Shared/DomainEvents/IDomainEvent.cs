namespace BakePopup.Domain.Shared.DomainEvents
{
    /// <summary>
    /// Represents a domain event in the system.
    /// </summary>
    public interface IDomainEvent
    {
        // Marker interface for domain events
    }
}
